<header class="hidden sm:fixed top-0 w-full text-white mx-auto z-50">
    <nav class="flex justify-between items-center px-6 py-4 max-w-[80%] mx-auto text-sm">
        <div class="hidden sm:flex flex-row gap-4 justify-start">
            <a href="/" class="uppercase font-medium inline-block">Cakes</a>
            <a href="/" class="uppercase font-medium inline-block">Cupcakes</a>
        </div>
        <div class="hidden sm:flex flex-row justify-end">
        <a href="/" class="inline-block">Order Cake</a>
        </div>
    </nav>
<header>

<style>
    header {
        background: transparent;
        animation: header-blur-scroll 0.3s ease-in-out both;
        animation-timeline: scroll();
        animation-range: 0 250px;
        border-bottom: 1px solid transparent;
    }

    @keyframes header-blur-scroll {
        0% {
            background: transparent;
            backdrop-filter: blur(0px);
        }
        100% {
            background: rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            padding-bottom: var(--spacing-6);
            border-bottom-color: red;

        }
    }
</style>